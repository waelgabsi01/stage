{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Titredestage</th>
                <td>{{ stage.titredestage }}</td>
            </tr>
            <tr>
                <th>Descriptiondestage</th>
                <td>{{ stage.descriptiondestage }}</td>
            </tr>
            <tr>
                <th>Datedebutstage</th>
                <td>{{ stage.datedebutstage ? stage.datedebutstage|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datefinstage</th>
                <td>{{ stage.datefinstage }}</td>
            </tr>
            <tr>
                <th>Dureedestage</th>
                <td>{{ stage.dureedestage }}</td>
            </tr>
            <tr>
                <th>Lieustage</th>
                <td>{{ stage.lieustage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stage_index') }}">back to list</a>

    <a href="{{ path('app_stage_edit', {'id': stage.id}) }}">edit</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
