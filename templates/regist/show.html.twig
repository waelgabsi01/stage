{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    <h1>Registration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registration.id }}</td>
            </tr>
            <tr>
                <th>Dateregistration</th>
                <td>{{ registration.dateregistration ? registration.dateregistration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ registration.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_regist_index') }}">back to list</a>

    <a href="{{ path('app_regist_edit', {'id': registration.id}) }}">edit</a>

    {{ include('regist/_delete_form.html.twig') }}
{% endblock %}
